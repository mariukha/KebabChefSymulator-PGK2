import unitypack

# Actually I can just create a small C# script in the project and run it via Editor, but wait, I can just use Unity's Editor CLI or write an Editor script.
# Wait! We can just use grep on the .fbx file to find the bone names!
